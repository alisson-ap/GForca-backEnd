const partidas = [];

const partida = {
    id:0,
    sockets: [],
    nivelRoom: 0,
    player1: [],
    player2: []
}

const postPartida = (players)=>{
    console.log(players);
}

module.exports ={
    postPartida
}